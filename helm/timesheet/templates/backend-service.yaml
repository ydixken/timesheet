apiVersion: v1
kind: Service
metadata:
  name: {{ include "timesheet.fullname" . }}-backend
  labels:
    {{- include "timesheet.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.backend.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "timesheet.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: backend
